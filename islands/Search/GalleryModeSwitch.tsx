import Component from "$store/components/search/GalleryModeSwitch.tsx";
import type { GalleryModeSwitchProps as Props } from "$store/components/search/GalleryModeSwitch.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
