import Component from "$store/components/ui/ZoomableImage.tsx";
import type { Props } from "$store/components/ui/ZoomableImage.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
